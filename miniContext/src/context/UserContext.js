import React from "react";
// .creates a React Context object named UserContext.
// Context provides a way to share values like state or functions between 
// components without explicitly passing props through every level of the component tree.
const UserContext = React.createContext()


export default UserContext;