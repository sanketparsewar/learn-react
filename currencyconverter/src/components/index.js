// this file is created because when we create multiple components the exporting each 
// and evey component can be complex so all the components are imported here and then exported

import InputBox from "./InputBox";
export {InputBox}
